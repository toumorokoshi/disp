let print-addition (fn [l r] [(print (+ l r))])
# macro! unless [cond body]
#   match cond {
#     false: body
#   }
macro! if [cond body]
	match cond {
    true: body,
  }

macro! ++ [var]
	let var (+ var 1)

macro! for [init cond end body] [init (while cond [body end])]
