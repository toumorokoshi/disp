defun when [[statement Statement] [block [List Statement]] [List Statement]
   return [if statement block]


when


defun foo {a: Int} Int
  return 5

defun foo {a: Int} Int
  return 5

foo {a: Int} -> Int

if [== 1 x]
  print "foo"


defmacro if [conditional:Conditional block:Block] None
  return [
    x = exec conditional
  ]


print "foo"
print "bar"

for s in statements:
  exec(scope, line)